{% extends "base.html" %}

{% block content %}
<TABLE WIDTH=100%>
 <TR> 
  <TH VALIGN=TOP ALIGN=Middle>
  <form action="" method="post">         {% csrf_token %}
   <label>Имя варианта</label> 
        <input name="name_var" type="text" value="{{ name_var }}"><br>   
        <label>Тип компонента:</label>
	<select name="type_comp">
	{% for vr in model_vr %}
          <option value="{{vr.id}}">{{vr.type_name}}</option>
	{% endfor %}
	</select>
	<label>Вероятность:</label>
	<select name="p">
	{% for var_p in model_p %}
          <option value="{{var_p.id}}">{{var_p.type_name}}</option>
	{% endfor %}
	</select>
	<br>
        <input class="btn info"  type="submit" value="Отправить"></form>
    </form>
  </TH>
  <TH ALIGN=CENTER>
	</TH>
 </TR>
</TABLE>
{% endblock %}
